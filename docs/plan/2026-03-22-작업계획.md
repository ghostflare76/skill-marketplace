# 2026-03-22 작업계획: 스킬 정렬 및 NEW 뱃지

## 배경
- 스킬 목록이 정렬 없이 표시됨
- 저장소 내 최근 7일 이내 Git 커밋된 스킬/에이전트/command에 NEW 표시 필요
- `lastUpdated` 필드가 항상 현재 시각을 반환하여 실제 커밋 날짜가 아님

## TODO 목록

### Phase 1: 타입 및 인터페이스 확장

- [ ] **1-1.** `src/lib/types.ts` — `Skill` 인터페이스에 `isNew: boolean` 필드 추가
- [ ] **1-2.** `src/lib/providers/types.ts` — `RepositoryProviderAdapter`에 optional 메서드 추가
  ```typescript
  getLastCommitDate?(repo: SkillRepository, filePath: string): Promise<string | null>;
  ```

### Phase 2: Provider별 커밋 날짜 조회 구현

- [ ] **2-1.** `src/lib/providers/github.ts` — `getLastCommitDate` 구현
  - `octokit.rest.repos.listCommits({ path, per_page: 1 })` 사용
  - 실패 시 `null` 반환
- [ ] **2-2.** `src/lib/providers/bitbucket.ts` — `getLastCommitDate` 구현
  - `{apiBase}/commits?path={filePath}&until={branch}&limit=1` 사용
  - `authorTimestamp` → ISO string 변환

### Phase 3: 서비스 레이어 통합

- [ ] **3-1.** `src/lib/skill-service.ts` — `isWithinDays` 유틸리티 함수 추가
  ```typescript
  function isWithinDays(dateStr: string, days: number): boolean
  ```
- [ ] **3-2.** `src/lib/skill-service.ts` — `parseSkillMd` 시그니처 변경
  - `lastCommitDate?: string` 파라미터 추가
  - `lastUpdated`에 실제 커밋 날짜 할당 (fallback: `new Date().toISOString()`)
  - `isNew` 계산: `lastCommitDate`가 7일 이내이면 `true`
- [ ] **3-3.** `src/lib/skill-service.ts` — `getAllSkills` 내 커밋 날짜 조회 통합
  - `entries.map` 내부에서 `provider.getLastCommitDate` 호출
  - `buildSkillFilePath` (constants.ts) 재사용하여 파일 경로 생성
  - 기존 1시간 캐시(all-skills) 안에 포함되므로 추가 캐시 불필요

### Phase 4: API 정렬

- [ ] **4-1.** `src/app/api/skills/route.ts` — 필터링 후 이름순 정렬 추가
  ```typescript
  skills.sort((a, b) => a.name.localeCompare(b.name, undefined, { sensitivity: 'base' }));
  ```

### Phase 5: UI NEW 뱃지

- [ ] **5-1.** `src/components/SkillCard.tsx` — NEW 뱃지 조건부 렌더링
  - 카드 헤더의 스킬 이름 옆에 배치
  - rose 색상 (`bg-rose-500/10 text-rose-400 border-rose-500/20`)
  - 기존 카테고리 뱃지 스타일 패턴 준수

### Phase 6: 검증

- [ ] **6-1.** `bun run build` — 타입 에러 없이 빌드 성공 확인
- [ ] **6-2.** `bun dev` — 로컬 실행 후 이름순 정렬 확인
- [ ] **6-3.** NEW 뱃지 표시 확인 (7일 이내 커밋 스킬)
- [ ] **6-4.** 7일 이전 스킬에 NEW 뱃지 미표시 확인
- [ ] **6-5.** 카테고리/검색 필터 적용 후에도 정렬 + NEW 뱃지 유지 확인

## 변경 파일 목록 (7개)

| # | 파일 | 변경 유형 | TODO |
|---|------|----------|------|
| 1 | `src/lib/types.ts` | 수정 | 1-1 |
| 2 | `src/lib/providers/types.ts` | 수정 | 1-2 |
| 3 | `src/lib/providers/github.ts` | 수정 | 2-1 |
| 4 | `src/lib/providers/bitbucket.ts` | 수정 | 2-2 |
| 5 | `src/lib/skill-service.ts` | 수정 | 3-1, 3-2, 3-3 |
| 6 | `src/app/api/skills/route.ts` | 수정 | 4-1 |
| 7 | `src/components/SkillCard.tsx` | 수정 | 5-1 |

## API 호출 비용

| 항목 | 현재 | 적용 후 | 비고 |
|------|------|---------|------|
| 캐시 미스당 호출 | N+S+3 | 2N+S+3 | N: 스킬 수, S: skills/ 디렉토리 수 |
| 50개 스킬 기준 | ~83회 | ~133회 | +50회 (listCommits per_page=1) |
| GitHub 한도 대비 | 1.7% | 2.7% | 5000회/h 기준 |
| 호출 주기 | 1시간 | 1시간 | 캐시 TTL 동일 |

## 의존 관계

```
Phase 1 (타입) → Phase 2 (Provider) → Phase 3 (서비스) → Phase 4 (API) → Phase 5 (UI) → Phase 6 (검증)
```

Phase 1~2는 병렬 가능, Phase 3은 1+2 완료 후 진행
